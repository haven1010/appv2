# 全流程跑通说明（老板/基地管理员/现场管理员/工人）

## 1. 角色分工

- 老板（`boss`）：只负责企业入驻资料提交（企业信息、营业执照、环境图、地址、工资、联系人）。
- 超级管理员（`super_admin`）：审核入驻、转交基地负责人、最终工资发放。
- 基地管理员（`base_manager`）：发布岗位、按签到生成工资草稿、查看工资统计。
- 现场管理员（`field_manager`）：现场扫码签到、查看报名/签到表。
- 工人（`worker`）：报名、出示二维码、确认工资、查看到账和历程。

## 2. 一键模拟（30人）

先启动后端：

```bash
cd app/backend
npm run start:dev
```

新开终端执行：

```bash
cd app/backend
SUPER_ADMIN_PHONE=你的超管手机号 SUPER_ADMIN_IDCARD_LAST6=你的超管身份证后6位 npm run simulate:full-flow
```

可选参数：

- `API_BASE`（默认 `http://127.0.0.1:3001/api`）
- `WORKER_COUNT`（默认 `30`）
- `WORK_DATE`（默认当天，如 `2026-04-01`）

## 3. 运行结果

脚本会自动完成：

1. 创建老板、基地管理员、现场管理员、30名工人账号。
2. 老板提交入驻基地。
3. 超管审核基地并转交基地负责人为基地管理员。
4. 基地管理员发布招聘岗位。
5. 30名工人自动报名（自动写入报名表）。
6. 现场管理员批量扫码签到（生成签到表）。
7. 基地管理员按签到批量生成当日工资草稿。
8. 工人确认工资。
9. 超级管理员发起支付、确认支付、完成发放（触发到账提醒）。
10. 验证工人“到账记录”和“工作历程”接口可读。

导出文件位置：

- `app/backend/exports/full-flow-时间戳/signup-table.csv`
- `app/backend/exports/full-flow-时间戳/checkin-table.csv`
- `app/backend/exports/full-flow-时间戳/salary-table.csv`
- `app/backend/exports/full-flow-时间戳/credentials.json`

`credentials.json` 含本次生成账号（手机号 + 身份证后6位）用于小程序登录测试。

## 4. 小程序端联调入口

- 工人端：
  - 首页“我要报名” -> 报名成功页
  - 二维码页出示上工码
  - 收入页确认工资
  - 我的 -> 银行卡页查看最近到账、工作历程查看历史
- 老板端：
  - `pages/boss/dashboard/dashboard` 提交企业资料
- 管理端：
  - 扫码页 `pages/admin/attendance/attendance`：报名/签到查看与导出、当日计薪草稿
  - 系统工资页 `pages/admin/system/system`：工资总表导出（超管）

